# Requirements for experiments/ (training, evaluation, plotting)
#
# Adjust the torch + torchvision versions to match your CUDA/CPU setup.
# For CUDA builds use the official PyTorch wheels from https://pytorch.org/

# Core (PyTorch)
torch>=1.10.0
torchvision>=0.11.0

# Model & utilities used in experiments
einops>=0.6.0          # tensor rearrange helpers used by ESEUNet
thop>=0.0.31.post100   # FLOPs / params profiler (used in model scripts)
torchinfo>=1.7.0       # optional model summary helper

# Data / transforms
albumentations>=1.1.0
imgaug>=0.4.0
opencv-python>=4.5.3.56
Pillow>=8.0.0

# Numerical & plotting
numpy>=1.19.0
matplotlib>=3.3.0

# Metrics / ML utilities
scikit-learn>=0.24.0
scikit-image>=0.18.0

# Dev / I/O / misc
tqdm>=4.50.0
tensorboard>=2.4.0

# Optional / advanced (fvcore used in some notebooks for FLOP counting)
fvcore>=0.1.5

# Notes:
# - Install a matching torch+torchvision for your CUDA version, e.g.:
#   pip install torch==1.12.1+cu116 torchvision==0.13.1+cu116 -f https://download.pytorch.org/whl/torch_stable.html
# - For CPU-only systems, install the CPU build of torch from the PyTorch index.
